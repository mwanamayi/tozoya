
<%= stylesheet_link_tag "feed" %>

<% if invited_events[0] %>
<section class="wrapper">
  <!-- <div class="inner"> -->
  <section class="spotlight">
    <div class="image">
      <%= image_tag(invited_events[0].user.avatar) %>
    </div>
    <div class="content">
      <h3><%=invited_events[0].name %></h3>
      <h5><%=invited_events[0].location_address %></h5>
      <p><%=invited_events[0].description %></p>
    </div>
  </section>
  <% end %>
  <% if invited_events[1] %>
  <section class="spotlight">
    <div class="image">
      <%= image_tag(invited_events[1].user.avatar) %>
    </div>
    <div class="content">
      <h3><%=invited_events[1].name %></h3>
      <h5><%=invited_events[1].location_address %></h5>
      <p><%=invited_events[1].description %></p>
    </div>
  </section>
  <% end %>
  <!-- </div> -->
</section>